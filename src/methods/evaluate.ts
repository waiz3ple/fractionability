import Fraction from '../core/Fraction';

export function evaluate(expression: string): Fraction {
  // Logic to evaluate expressions like '3 * 1/6'
  // For now, parse the expression and return a default Fraction instance
  console.log(expression); // Use the expression parameter
  return new Fraction(0);
}